{{#if transferPanel}}
    <p>{{t 'domain.unlink_previous_owner'}} <b>{{addressRecord}}</b></p>
    {{#if multipleTransactions}}
        <p>{{t 'domain.multiple_transactions'}} {{transactionCount}} {{t 'domain.multiple_transactions_end'}}</p>
    {{/if}}
    {{#if commitingToTransfer}}
    {{else}}
        <img class="btn-icon-global right-margin" {{on 'click' toggleTransferPanel}} src="/assets/img/cancel-small.png" />
    {{/if}}
        {{#if transfering}}
            <img width="40" src="/assets/img/loader-square-fixed-small.png" />
        {{else if commitingToTransfer}}
            <img width="40" src="/assets/img/see-mm-small.png" />
        {{else}}
            <img class="btn-icon-global" {{on 'click' unassignPreviousOwner}} src="/assets/img/ok-small.png" />
        {{/if}}
{{else}}
    <img class="cursor float" {{on 'click' toggleTransferPanel}} src="/assets/img/unlink-button-small.png"/>
{{/if}}
