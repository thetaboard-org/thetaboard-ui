<nav class="navbar navbar-expand-lg navbar-absolute navbar-transparent">
    <div class="container-fluid">
        <div class="navbar-wrapper">
        <div class="navbar-toggle d-inline" id="toggler-navigation">
            <button type="button" class="navbar-toggler">
            <span class="navbar-toggler-bar bar1"></span>
            <span class="navbar-toggler-bar bar2"></span>
            <span class="navbar-toggler-bar bar3"></span>
            </button>
        </div>
        </div>
        <div class="collapse navbar-collapse" id="navigation">
            <ul class="navbar-nav ml-auto">
                <li class="search-bar input-group">
                <button class="btn btn-link" id="search-button" data-toggle="modal" data-target="#searchModal"><i class="tim-icons icon-zoom-split"></i>
                  <span class="d-lg-none d-md-block">Search</span>
                </button>
              </li>
                <li class="dropdown nav-item">
                    {{#if this.session.isAuthenticated}}
                        <a href="#" class="dropdown-toggle nav-link" data-toggle="dropdown" aria-expanded="false">
                        <div class="photo">
                            <img src="{{rooURL}}/assets/img/avatar.jpg" alt="Profile Photo">
                        </div>
                        </a>
                        <ul class="dropdown-menu dropdown-navbar">
                            {{#if this.currentUser.user}}
                                <li class="nav-link"><a href="javascript:void(0)" class="nav-item dropdown-item">Signed in as<br/>{{this.currentUser.user.email}}</a></li>
                                <li class="dropdown-divider"></li>
                                <li class="nav-link"><LinkTo @class="nav-item dropdown-item" @route="my-wallets"> My Wallet</LinkTo></li>
                            {{/if}}
                            <li class="dropdown-divider"></li>
                            <li class="nav-link"><a href="javascript:void(0)" {{on "click" this.invalidateSession}} class="nav-item dropdown-item logout">Log out</a></li>
                        </ul>
                    {{else}}
                        <LinkTo @route="login" @class="btn btn-fill btn-primary" style="padding: 7px 20px;margin-top: 10px;">Login</LinkTo>
                    {{/if}}
                </li>
            </ul>
        </div>
    </div>
</nav>