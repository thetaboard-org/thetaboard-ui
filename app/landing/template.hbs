{{page-title "Landing"}}

<div class="landing-top-container">
    <h1 class="landing-title" style="font-family: azonix;">Thetaboard</h1>
    <h4 class="landing-subtitle" style="font-family: arcon;">{{t 'landing.title'}}</h4>
    <p class="" style="font-family: arcon;">{{{t 'landing.subtitle'}}}</p>
    <h3 style="margin-top: 110px">{{t 'landing.get_started'}}</h3>
    <div class="tile-container-market">
         <div style="background: none;" class="card tile-nft-marketplace tile-landing">
            <img src="{{rooURL}}/assets/nft/tns_placeholder.png" alt="{{t 'landing.tns'}}"/>
            <LinkTo @route="domain.search" class="btn btn-primary btn-landing">{{t 'landing.tns'}}</LinkTo>
        </div>
         <div style="background: none;" class="card tile-nft-marketplace tile-landing">
            <img src="{{this.model.nft.image}}" alt="{{t 'landing.last_drop'}}" />
           <div class="tile-landing-text">{{this.model.nft.name}}</div>
            <LinkTo @route="marketplace.drops" class="btn btn-primary btn-landing">{{t 'landing.last_drop'}}</LinkTo>
        </div>
         <div style="background: none;" class="card tile-nft-marketplace tile-landing">
            <img src="{{this.model.marketplaceNewlyAdded.image}}" alt="{{t 'landing.marketplace'}}"/>
           <div class="tile-landing-text">{{this.model.marketplaceNewlyAdded.name}}</div>
            <LinkTo @route="marketplace.secondary" class="btn btn-primary btn-landing">{{t 'landing.marketplace'}}</LinkTo>
        </div>
        <div style="background: none;" class="card tile-nft-marketplace tile-landing">
            <img src="{{rooURL}}/assets/img/landing-dashboard.png" alt="{{t 'landing.holdings'}}"/>
            <LinkTo @route="dashboard" class="btn btn-primary btn-landing">{{t 'landing.holdings'}}</LinkTo>
        </div>
    </div>
</div>